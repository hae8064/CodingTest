function solution(arr) {
    var total = 0;      //덧셈 합
    for(var i = 0; i < arr.length; i++){
        total += arr[i] 
    }
    var answer = 0;
    answer = total / arr.length;
    return answer;
}
