function solution(x, n) {
    var answer = [];
    var total = 0;
    
    for(let i = 0; i < n; i++){
        total += x;
        answer.push(total);
    }
    return answer;
}

// x = prompt("정수를 입력하세요:");
// n = prompt("자연수를 입력하세요:");

// solution(x,n);
